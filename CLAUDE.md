# Bronze — Claude Code

Read `AGENTS.md` first. It is the project contract for every agent.

For Settings, Library, Help, queue, CSS, typography, motion, or any
Apple-like / HIG request, read `hig/SKILL.md` and follow that workflow.
The canonical library is `hig/`. `.claude/skills/apple-hig/` is only a
pointer.

Do not run Hedgehog `full-stack-app` / `pwa-app` / `landing-page` cores.
Do not resume `bmad-loop` run `6a79`.
