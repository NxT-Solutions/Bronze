{
  "name": "bronze",
  "private": true,
  "packageManager": "pnpm@12.4.2",
  "scripts": {
    "lint": "TURBO_TELEMETRY_DISABLED=1 turbo run lint",
    "typecheck": "TURBO_TELEMETRY_DISABLED=1 turbo run typecheck",
    "test": "TURBO_TELEMETRY_DISABLED=1 turbo run test",
    "validate": "TURBO_TELEMETRY_DISABLED=1 turbo run validate",
    "verify": "TURBO_TELEMETRY_DISABLED=1 biome check . && TURBO_TELEMETRY_DISABLED=1 turbo run lint typecheck test validate && cargo fmt --check && cargo clippy --workspace -- -D warnings && cargo test --workspace && python3 tooling/planning-checks.py"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.14",
    "@tauri-apps/cli": "2.11.5",
    "turbo": "2.11.4"
  },
  "engines": {
    "node": "24.21.0"
  }
}
