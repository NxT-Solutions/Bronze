{
  "name": "bronze",
  "private": true,
  "packageManager": "pnpm@12.4.2",
  "scripts": {
    "lint": "turbo run lint",
    "verify": "biome check . && turbo run typecheck && turbo run test && turbo run validate && cargo fmt --check && cargo clippy --workspace -- -D warnings && cargo test --workspace && python3 tooling/planning-checks.py"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.14",
    "@tauri-apps/cli": "2.11.4",
    "turbo": "2.10.12"
  },
  "engines": {
    "node": "24.21.0"
  }
}
